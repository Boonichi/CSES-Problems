#include <bits/stdc++.h>

using namespace std;
#define ll long long
long long M = 1e9 + 7;
int main(){
    freopen("input.inp","r",stdin);
    freopen("output.out","w",stdout);
    ios::sync_with_stdio(0);
    cin.tie(0);
    ll x; int n;
    cin >> x >> n;
    multiset<ll> ms;
    ms.insert(0);
    ms.insert(x);
    for (int i = 0; i < n; i++){
        ll res = 0;
        ll x;
        cin >> x;
        it 
    }
    return 0;
}